# 数据预处理与前置分析口径说明

## 1. 说明目的

本文档单独记录数据预处理与前置分析阶段的口径确认过程，并解释每项选择的含义、采用理由、影响范围和检查方式。具体实施步骤以《数据预处理与前置分析实施方案》为准，本文档不替代实施清单。

---

## 2. 已确认问题完整清单

| 序号 | 确认问题 | 讨论过的主要口径 | 最终口径 |
|---:|---|---|---|
| 1 | 数据列与结果模板如何对应 | 按模板同序映射；将数据时刻解释为前一区间终点 | 按官方模板同序映射 |
| 2 | 问题 2～4 是否严格遵守决策时点的信息边界 | 严格滚动；全年离线训练；直接使用同日实际值 | 严格滚动 |
| 3 | 问题 2～4 的 SOC 如何处理 | 全年连续且闭合；连续并设置终值；每日首尾相等 | 全年连续且首尾闭合 |
| 4 | 题面“充放电效率 90%”如何解释 | 充、放电各 90%；往返总效率 90%；完整循环只计一次 90% | 充电效率和放电效率各为 90% |
| 5 | 调整购电费用如何结算 | 保留量与差额分段净结算；原计划全额付费后另加差额费用 | 分段净结算 |
| 6 | “调整购电量”工作表记录什么 | 最终调整后计划；相对计划的净差额；每次调整明细 | 最终调整后的生效计划 |
| 7 | 附件 3 小时预报如何转换到 10 分钟 | 线性插值；分段常值 | 线性插值 |
| 8 | 前置分析交付到什么程度 | 完整前置包；仅清洗与 EDA；同时完成最终预测模型 | 竞赛完整前置包 |
| 9 | 是否引入外部数据 | 只用附件；加入节假日；加入天气和节假日 | 只使用附件 |
| 10 | 是否使用日历特征 | 模型禁用但分析可用；分析和模型均禁用 | 分析和模型均不使用日历特征 |
| 11 | 每日 00:00～00:10 如何归属 | 由前一日滚动计划承接；强制重新排列为日历日 | 由前一日计划末段承接 |
| 12 | 每日计划覆盖什么时间范围 | 00:10 至次日 00:10；重排为 00:00 至 24:00 | 00:10 至次日 00:10 |
| 13 | 问题 3 调整时是否更新负载预测 | 保持日初负载预测；利用当日观测同步更新 | 保持 0:00 负载预测不变 |
| 14 | 使用什么实施技术栈 | Python；MATLAB；Python 与 MATLAB 混合 | Python 数据管线 |
| 15 | 当天实际负载和光伏如何使用 | 仅结算与回测；直接用于日初计划；主方案滚动并另设完美信息基准 | 仅用于结算与回测 |
| 16 | 前置阶段是否生成预测输出 | 生成可运行基线；只生成预测特征；直接预测净负荷 | 生成可运行基线并保留特征 |
| 17 | 哪些量需要预测 | 问题 2 的负载、光伏或净负荷；问题 3 的负载是否仍需预测 | 问题 2 分别预测负载和光伏；问题 3 只预测负载 |
| 18 | 简单预测基线使用多长窗口 | 最近 7 天；最近 14 天；截至当日全部历史 | 最近 7 天相同时段均值 |
| 19 | 1 月初历史不足时如何冷启动 | 附件 1 作先验；用不足 7 天的历史；用 1 月同日实际值 | 首日用附件 1，随后逐步改用已有历史 |
| 20 | SOC 末端闭合到哪个时刻 | 2026-01-01 00:10；2026-01-01 00:00 | 闭合到 2026-01-01 00:10 |

此外，根据附件 3 与实际光伏的对齐检验，统一采用以下数据解释：`预报 h 小时` 的目标时刻为 `发布时间 + h 小时`，其中“预报 1 小时”对应发布时间后一小时，而不是发布时间当小时。

---

## 3. 各项口径的详细说明

## 3.1 按官方模板同序映射

### 含义

附件中的第 `i` 个时段值与结果模板中的第 `i` 个购电区间直接对应，不因个人对“时刻代表区间起点还是终点”的理解而移动列。

例如，原始数据首个时刻 `00:10` 与结果模板首个区间 `0:10-0:20` 对应；末列 `0:00+1` 与模板的跨日末段对应。

### 采用理由

官方模板是最终提交接口。若将原始列整体前移或后移，即使内部模型自洽，也可能造成论文指定时段、结果模板和原始数据之间错位。保持列序能够最大限度降低提交格式风险。

### 影响

- 所有中间表必须保留 `slot_index=1,…,144`；
- 最终回填以 `slot_index` 为主键；
- 时间戳只用于运行逻辑，不替代模板列序；
- 10:00-10:10、23:50-24:00 和跨日末列必须单独抽查。

### 验证

随机抽取多个时段，检查原始列号、长表 `slot_index`、结果模板列号和显示标签是否一一对应。

## 3.2 滚动计划时域与跨日衔接

### 含义

日期 `d` 的新计划覆盖 `d 00:10` 至 `d+1 00:10`，共 144 个 10 分钟区间。日历日开始后的首段 `d 00:00–00:10` 由前一日计划的最后一段负责。

### 采用理由

附件和模板均从 `00:10` 开始，并在末端延伸到次日 00:10。将其解释为滚动 24 小时计划，可以同时保留官方列序和时间连续性。

若强行将每行解释为 00:00～24:00，会造成首末列移动、跨日重复或遗漏，并影响 SOC 和费用汇总。

### 影响

- 计划日期与区间实际所属日历日期不完全相同；
- 每日计划总量按该计划行 144 段求和；
- 0:00、24:00 SOC 必须从全局状态序列提取；
- 后续预报调整必须基于真实时间戳截取尚未执行区间。

### 验证

- 每个计划日恰好 144 段；
- 相邻区间严格连续；
- 前一日末段终点等于后一日首段起点；
- 全年不存在重复或缺失的 10 分钟区间。

## 3.3 严格的信息可得性

### 含义

每天 0:00 制定计划时，只允许使用当时已经给出的附件信息和此前已经发生的实际数据。6:00、12:00、18:00 调整时，只允许加入该时刻已经发布的新预报和已经实现的系统状态。

### 采用理由

题目的核心是“提前制定策略”。如果日初计划使用当天未来实际负载或光伏，就等于提前知道未来，所得结果属于完美信息回算，不是可执行策略。

严格的信息边界还能避免：

- 紧急购电量被人为压低；
- 预测误差被错误消除；
- 问题 2 与问题 3 的差异失去意义；
- 论文被判定为数据泄漏或时序逻辑错误。

### 影响

所有预测数据必须保存 `issue_ts`、`target_ts`、`training_start` 和 `training_end`，并满足 `training_end < issue_ts`。

### 验证

为每条预测执行时间审计；任何训练记录日期不早于发布时间时，质量检查直接失败。

## 3.4 实际负载和光伏仅用于结算与回测

### 含义

附件 2 的当天实际值不进入当天 0:00 的计划输入，而用于：

- 模拟计划执行；
- 计算实际供电缺口；
- 确定紧急购电；
- 更新真实 SOC；
- 评价预测误差和策略费用；
- 进入后续日期的历史窗口。

### 采用理由

实际值是事后观测。将其用于计划会破坏信息时序，但完全不使用实际值又无法计算紧急购电和实际费用。因此应严格区分“计划输入”和“事后结算输入”。

### 验证

计划输入表不得包含同日 `load_actual` 或 `pv_actual` 的直接引用；仿真结算表则必须能够回溯到附件 2。

## 3.5 哪些问题需要预测，以及为什么需要预测

### 问题 1

题目已经给出代表性负载、电价和光伏预测，不需要额外预测。

### 问题 2

每天 0:00 需要提前申报未来 24 小时计划，但附件 2 只有实际负载和实际光伏。因此需要用此前数据估计：

- 未来负载；
- 未来光伏。

也可以从数学上直接预测净负荷，但最终选择分别预测负载与光伏，以保持物理含义清楚，并与问题 3 的数据接口一致。

### 问题 3

光伏使用附件 3 已给出的预报，不再自行预测。附件没有提供负载预报，因此仍需生成日初负载预测。

### 问题 4

问题 4 只改变价格数据，预测信息结构分别继承问题 2 和问题 3。

### 采用理由

预测只是产生决策输入的必要步骤，最终目标仍是优化购电策略。采用简单、透明的预测基线可以避免将赛题重心错误转移到复杂黑箱预测模型。

## 3.6 最近 7 天相同时段均值基线

### 含义

日期 `d` 的某个 10 分钟时段，使用 `d-7` 至 `d-1` 相同时段实际值的算术均值作为预测。

问题 2 分别生成负载和光伏基线；问题 3 只使用负载基线。

### 采用理由

- 严格使用历史信息；
- 不需要天气或日历特征；
- 能跟随近期水平变化；
- 比单日持续法更稳定；
- 比全部历史均值对近期变化更敏感；
- 便于解释和复现。

### 为什么选择 7 天而不是 14 天或全部历史

14 天及全部历史会提高平滑程度，但也会削弱对短期负载与光伏水平变化的响应。7 天在稳定性和响应速度之间取得较简单的平衡，而且 2 月 1 日前已有完整历史窗口。

### 影响

该基线用于跑通完整链路，不代表最终预测方法已经锁死。后续可以在保持字段接口不变的情况下加入其他预测列。

### 验证

随机抽取日期和时段，独立读取此前 7 天原值并复算均值；同时检查窗口中没有当日或未来记录。

## 3.7 冷启动使用附件 1

### 含义

- 2025-01-01 使用附件 1 的负载和光伏代表性曲线；
- 1 月 2 日使用此前 1 天实际值；
- 1 月 3 日使用此前 2 天均值；
- 依次增加历史长度；
- 1 月 8 日起固定使用最近 7 天。

### 采用理由

1 月 1 日没有此前实际数据，必须给出可执行的初始估计。附件 1 是题目正式提供的先验信息，而且其曲线与全年相同时段均值基本一致，适合作为冷启动基准。

直接使用 1 月当天未来实际值虽然方便，但会破坏已确认的信息边界。

### 影响

1 月同时承担预测冷启动和 SOC 热身作用；正式提交从 2 月 1 日开始，此时已经具有完整 7 天历史。

### 验证

在预测结果中保存 `history_days` 和 `forecast_source`。1 月 1 日应标记为附件 1 先验，1 月 2～7 日应显示逐渐增加的历史天数。

## 3.8 不使用外部数据和日历特征

### 含义

不引入天气、辐照度、节假日或其他外部数据；同时不在分析或模型中使用月份、星期几、工作日/周末等日历特征。

### 采用理由

- 保证方案完全基于题目附件；
- 避免外部数据地区、时间和口径无法对应；
- 保持方法简单且容易复现；
- 防止前置分析范围无控制扩张。

### 日期仍然保留的用途

日期仍必须用于时间排序、历史窗口、跨表连接、结果期标识、重点日期提取和信息泄漏检查。日期只是索引，不作为预测解释变量或分析分组字段。

### 验证

最终预测特征表不得出现月份、星期、周末、节假日等字段；分析报告也不得按这些类别分组。

## 3.9 附件 3 的预测步长解释

### 含义

对发布时间 `issue_ts`，第 `h` 个预测值的目标时刻定义为：

```text
target_ts = issue_ts + h 小时
```

因此“预报 1 小时”对应发布后一小时。

### 采用理由

将附件 3 与附件 2 实际光伏分别按“发布后 1 小时”和“发布当小时”对齐后，前者误差显著更小，能够确认预测步长语义。

### 影响

任何偏移一小时的解释都会导致预测误差、调整时点和购电区间整体错位。

### 验证

对四个发布时间分别抽样，检查预测目标时间；同时比较两个候选偏移的误差，确保采用误差合理的对齐方式。

## 3.10 小时预报线性插值到 10 分钟

### 含义

以发布时间当刻已经观测到的光伏为起点，以未来 1～24 小时整点预报为后续锚点，在相邻锚点间进行线性插值，每次发布时间生成未来 144 个 10 分钟值。

### 采用理由

- 10 分钟优化必须有同粒度输入；
- 线性插值比每小时复制常数更连续；
- 不引入额外预测模型；
- 保留官方预报整点值；
- 方法透明，便于论文说明。

### 边界处理

- 插值结果不得小于零；
- 整点处必须严格等于附件 3 原值；
- 2025-12-31 超出实际数据范围的预测保留，但不参与误差评价；
- 发布时间当刻的实际光伏可以使用，因为它在决策时已经发生。

### 验证

检查每个发布时间生成 144 条记录、相邻时刻相差 10 分钟、整点锚点完全一致以及所有预测值非负。

## 3.11 问题 3 保持负载预测不变

### 含义

6:00、12:00、18:00 调整时，只更新尚未执行区间的光伏预报；负载仍使用当天 0:00 生成的预测。

### 采用理由

问题 3 明确新增的是光伏预报信息。若负载预测也同步更新，则成本变化同时受到两种新信息影响，无法判断额外光伏预报是否值得使用。

固定负载预测可以形成控制变量，使不同调整策略之间的比较具有可解释性。

### 影响

- 已执行区间不允许改变；
- 剩余区间只替换光伏预测版本；
- 负载预测版本号始终指向当日 0:00；
- 策略效果差异可归因于光伏预报更新和相应调整费用。

### 验证

对同一计划日比较四个决策版本：剩余区间的负载预测必须完全相同，光伏预测允许变化。

## 3.12 Python 数据管线

### 含义

使用 Python 完成数据读取、转换、校验、基线预测、图表和报告生成。主要依赖为 pandas、NumPy、openpyxl、PyArrow、Matplotlib 和 Seaborn。

### 采用理由

- 适合批量读取和整理 Excel；
- 长表和时间索引处理成熟；
- Parquet 能保存明确的数据类型并提高后续读取效率；
- 便于自动测试与 HTML 报告生成；
- 后续无论采用何种优化求解器，均可读取统一数据接口。

### 影响

原始附件解析只在数据管线中进行一次，后续优化代码不得重复编写各自的 Excel 清洗逻辑。

### 验证

在全新输出目录中运行管线，应能仅凭原始附件重新生成所有处理结果，并得到相同的行数、哈希和统计摘要。

## 3.13 SOC 全年连续

### 含义

储能状态从 2025-01-01 初始值开始，按照真实时间顺序持续传递，不在每日 0:00 重置。

### 采用理由

每日重置会等价于每天免费获得固定初始电量，切断前后两日的物理联系，并夸大储能的经济收益。

题面只在问题 1 中明确要求当日首末 SOC 相等，问题 2～4 更合理的处理是跨日连续。

### 影响

- 前一天末状态是下一天初状态；
- 1 月需要参与运行热身；
- 每日优化不能彼此完全独立；
- 结果表中的 0:00、24:00 SOC 应来自同一条全局状态序列。

### 验证

对每个日界线检查前一日 24:00 SOC 与后一日 0:00 SOC 完全相等。

## 3.14 SOC 闭合到 2026-01-01 00:10

### 含义

2025-01-01 0:00 的初始 SOC 为 6000 kWh。首个 00:00～00:10 作为衔接段，SOC 保持不变；覆盖所有计划行后，在 2026-01-01 00:10 将 SOC 约束回 6000 kWh。

### 采用理由

12 月 31 日结果行的最后一个计划区间延伸到次日 00:10。若只约束到次日 00:00，最后一个区间处于终端条件之外，可能出现无代价放电。

末端闭合还可以保证不同问题和价格情形在相同储能终值下公平比较。

### 影响

全年状态序列比自然日历年多覆盖最后一个 10 分钟计划段。

### 验证

同时检查初始状态和最终状态均为 6000 kWh，并确认最终状态位于最后一条计划记录之后。

## 3.15 充电和放电效率各为 90%

### 含义

充电效率取 0.9，放电效率取 0.9，完整充放电循环的往返效率为 0.81。

若充电量定义为交流侧进入储能的电量、放电量定义为储能向微网输出的电量，则：

- 充入 1 kWh，SOC 增加 0.9 kWh；
- 对外放出 1 kWh，SOC 减少 `1/0.9` kWh。

### 采用理由

题面“充放电效率为 90%”存在往返效率与双向效率两种可能解释。采用双向各 90% 后，能量流向和状态变化定义清楚，也便于逐项检查。

### 影响

该口径会直接影响可用电量、购电量、充放电时机和费用，因此配置文件、模型和论文必须完全一致。

### 验证

设计一组 1 kWh 充电和 1 kWh 放电的单步测试，分别核对 SOC 增量和减量。

## 3.16 调整购电采用分段净结算

### 含义

设初始计划购电量为 `plan`，最终调整后购电量为 `adjusted`，交易时刻电价为 `price`：

```text
down = max(plan - adjusted, 0)
up   = max(adjusted - plan, 0)

保留部分费用 = price × min(plan, adjusted)
下调违约费用 = 0.5 × price × down
上调新增费用 = 1.5 × price × up
```

若最终仍然供电不足，紧急购电另按 `5 × price` 计算。

### 采用理由

该口径对应“保留部分正常购买、取消部分支付违约费、新增部分按高价购买”的交易逻辑，避免原计划全额付费后再次对同一差额重复收费。

如果采用“原计划全额付费再加下调违约费”，下调通常不再具有经济意义，也不利于解释题目设置调整机制的目的。

### 影响

总费用必须分为计划保留费用、下调违约费用、上调新增费用和紧急购电费用，不能只输出一个无法复算的总数。

### 验证

分别设计不调整、仅下调、仅上调和同时包含不同区间上下调的测试，检查费用分项与总费用。

## 3.17 “调整购电量”保存最终生效计划

### 含义

官方工作表中的每个区间记录历次预报更新后最终执行的购电量。

### 采用理由

官方模板只有一张按日、按区间排列的调整表，无法自然保存 6:00、12:00、18:00 三个完整版本。保存最终量最便于执行、平衡校验和费用复算。

### 不包含

- 相对原计划的差额；
- 每次调整的完整历史表；
- 仅最后一次调整新增的数量。

调整历史应保存在内部日志中，用于论文分析和程序审计。

### 验证

检查最终量满足：原计划减累计下调、加累计上调，并与最后生效版本一致。

## 3.18 竞赛完整前置包

### 含义

前置阶段不仅完成数据清洗，还输出：

- 数据契约；
- 标准化长表；
- 日初基线预测；
- 10 分钟光伏预报；
- 数据质量报告；
- 描述性统计表；
- 前置分析图；
- 光伏预报误差分析；
- 四个重点日期核查；
- 可供优化程序直接读取的数据接口。

### 采用理由

国赛论文中的数据处理不能只写“经过预处理得到数据”。完整前置包能够形成可展示、可复算的证据，同时提前暴露时间错位、单位错误和数据泄漏问题。

### 边界

该阶段只评价数据和预报，不产生正式优化策略或问题结论。

## 3.19 数据质量检查与异常值政策

### 含义

对文件结构、时间连续性、主键唯一性、数值范围、单位转换、预测目标映射和信息边界执行自动检查。

异常值采用“标记但不自动删除”的政策。

### 采用理由

微网数据中的尖峰可能是真实的负荷高峰、光伏波动或价格波动。没有外部证据时直接平滑或缩尾，会改变供需缺口和费用结果。

夜间光伏零值属于有效观测，不能作为缺失值填补。

### 验证重点

- 主表 52,560 行；
- 正式结果期 48,096 个区间；
- 小时光伏预报长表 35,040 行；
- 10 分钟光伏预报表 210,240 行；
- 每个计划日 144 段；
- 数值字段无非法空值或无穷值；
- 所有预测训练数据早于发布时间。

## 3.20 前置分析措施

### 代表性日分析

比较附件 1 与全年相同时段均值，用于说明问题 1 的代表性输入及固定价格来源。全年均值只能用于回顾性说明，不能成为某日提前知道的实际数据。

### 负载、光伏和净负荷分析

分析 144 个时段的平均曲线、总体分布、光伏剩余比例和峰值错位。净负荷定义为负载减光伏，负值必须保留。

### 电价分析

比较附件 1 固定价格和附件 4 波动价格的峰谷差、离散程度及其与净负荷的关系，为问题 4 的结果解释准备依据。

### 预报误差分析

误差统一定义为“预测值减实际值”。计算 MAE、RMSE、偏差、绝对误差分位数以及高估、低估比例，并按预测步长和发布时间分析。

判断新增预报是否有价值时，必须比较同一交付时刻的新旧预报，不能直接比较不同发布时间的总体误差，因为它们覆盖的昼夜结构不同。

### 四个重点日期核查

单独检查 2025-03-20、2025-06-21、2025-09-23、2025-12-21 的负载、光伏、净负荷、电价和各版本预报，确保论文表格与最终结果能够从统一数据接口无歧义提取。

### 不使用日历分组

按照已确认口径，前置分析不按月份、星期或节假日分组。允许按计划时段、预测步长、发布时间和数值区间分析。

---

## 4. 口径之间的逻辑关系

这些口径并非彼此独立，而是形成一条完整的数据逻辑链：

```text
官方模板同序映射
        ↓
建立00:10至次日00:10的滚动时间轴
        ↓
实际值只用于执行和回测
        ↓
用历史7日均值形成日初输入
        ↓
问题3用附件3更新光伏、保持负载不变
        ↓
在全年连续SOC和统一效率下执行策略
        ↓
按分段净结算计算调整费用
        ↓
将最终生效购电量写入官方模板
```

任意修改其中一项，均可能影响后续接口。例如，若改变时段映射，预测目标时刻、SOC 日界线、费用汇总和结果回填都必须同步改变；若允许使用同日实际值，则基线预测、紧急购电和问题 3 的预报价值分析都会失去原有含义。

---

## 5. 论文撰写时的使用方式

在论文“问题分析”中说明：

- 每天的计划属于提前决策；
- 实际负载和光伏只能用于事后执行；
- 问题 3 的新增信息仅为光伏预报；
- 四问共享统一的时间轴和储能状态。

在论文“数据预处理”中说明：

- 原始宽表如何转为长表；
- `0:00+1` 和跨日时段如何处理；
- 功率如何换算为电量；
- 附件 3 日期如何填充；
- 小时预报如何线性插值；
- 如何防止未来信息泄漏。

在论文“模型假设”中说明：

- 充、放电效率各为 90%；
- 不考虑向外网售电；
- 允许光伏剩余通过储能或弃光处理；
- 问题 2～4 的 SOC 全年连续并设置末端闭合；
- 调整购电采用分段净结算。

在论文“模型评价”中说明：

- 简单基线的作用是提供可复现决策输入；
- 预报误差按相同目标时刻公平比较；
- 不将预测精度等同于最终经济效果；
- 是否采用后续预报最终应由总费用、紧急购电量和运行可行性共同判断。
