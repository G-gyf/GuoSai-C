# 数据预处理与前置分析口径说明

## 1. 说明目的

本文档单独记录数据预处理与前置分析阶段的口径确认过程，并解释每项选择的含义、采用理由、影响范围和检查方式。具体实施步骤以《数据预处理与前置分析实施方案》为准，本文档不替代实施清单。

---

## 2. 已确认问题完整清单

| 序号 | 确认问题 | 核心判据 | 最终口径 |
|---:|---|---|---|
| 1 | 数据列与结果模板如何对应 | 先判定物理时刻语义，再建立显式映射 | 负荷/光伏按区间终点，电价按区间起点，模板单独错位映射 |
| 2 | 问题 2～4 是否严格遵守决策时点的信息边界 | 决策输入必须在发布时间前可得 | 严格滚动 |
| 3 | 问题 2～4 的 SOC 如何处理 | 储能状态必须沿真实时间连续传递 | 跨日连续，不按日重置 |
| 4 | 题面“充放电效率 90%”如何解释 | 充电与放电能量流分别定义 | 充电效率和放电效率各为 90% |
| 5 | 调整购电费用如何结算 | 各类交易量不得重复计费 | 分段净结算 |
| 6 | “调整购电量”工作表记录什么 | 模板应反映最终生效计划 | 最终调整后的生效计划 |
| 7 | 附件 3 小时预报如何转换到 10 分钟 | 保留整点锚值并形成连续输入 | 线性插值 |
| 8 | 前置分析交付到什么程度 | 应形成可复算、可审计的数据接口 | 竞赛完整前置包 |
| 9 | 是否引入外部数据 | 结论不得超出题目数据支持范围 | 只使用附件 |
| 10 | 是否使用日历特征 | 只保留具有明确物理含义的季节变量 | 光伏仅允许年内 sin/cos 物理相位；星期、节假日和外部日历特征禁用 |
| 11 | 源日期的物理时间范围 | 负荷/光伏列名是区间终点 | `d 00:00` 至 `d+1 00:00` |
| 12 | 结果模板如何取值 | 计划区间与负荷/光伏终点、价格起点同时对齐 | 模板首行取 `00:20` 负荷/光伏与 `00:10` 电价，后续依次顺移 |
| 13 | 问题 3 调整时是否更新负载预测 | 仅考察新增光伏预报的作用 | 保持 0:00 负载预测不变 |
| 14 | 使用什么实施技术栈 | 需支持 Excel、时间索引和自动校验 | Python 数据管线 |
| 15 | 当天实际负载和光伏如何使用 | 实际值在目标区间结束后才可观测 | 仅用于结算与回测 |
| 16 | 前置阶段是否生成预测输出 | 优化程序需要可直接读取的因果输入 | 生成可运行基线并保留特征 |
| 17 | 哪些量需要预测 | 分别保留供需两侧信息 | 问题 2 分别预测负载和光伏；问题 3 只预测负载 |
| 18 | 简单预测基线使用多长窗口 | 兼顾近期性与样本稳定性 | 最近 7 天相同终点时刻均值 |
| 19 | 1 月初历史不足时如何冷启动 | 只使用决策前已经存在的信息 | 首日用附件 1，随后逐步改用已有历史 |

此外，根据附件 3 与实际光伏的对齐检验，统一采用以下数据解释：`预报 h 小时` 的目标时刻为 `发布时间 + h 小时`，其中“预报 1 小时”对应发布时间后一小时，而不是发布时间当小时。

---

## 3. 各项口径的详细说明

## 3.1 物理时间语义与模板错位映射

### 含义

附件 1、2 中的负荷与光伏时刻表示前一 10 分钟区间的终点，电价时刻表示购电区间的起点。因此，结果模板中 `00:10–00:20` 的购电计划，应与 `00:20` 的负荷/光伏以及 `00:10` 的电价共同进入同一平衡约束。模板后续区间按相同规则顺次映射。

### 采用理由

购电计划针对一个未来交付区间，供需平衡应使用该区间结束时观测的负荷和光伏，并按区间开始时公布的电价计费。分别保存物理时间轴与模板位置，可以同时保证能量平衡的时间一致性和最终回填的可追溯性。

### 影响

- 日历物理表保留源行 `slot_index=1,…,144`；
- 同时保存 `interval_start`、`interval_end`、`observation_ts`、`template_plan_date` 与 `template_slot_index`；
- 预测评价按 `observation_ts` 连接实际光伏；
- 购电约束按模板区间连接对应终点负荷/光伏和起点电价。

### 验证

重点核对：日历区间 `00:00–00:10` 对应 `00:10` 负荷/光伏；模板区间 `00:10–00:20` 对应 `00:20` 负荷/光伏和 `00:10` 电价；其余区间满足同一映射规律。

## 3.2 日历物理时间轴

### 含义

源日期 `d` 的 144 个负荷/光伏观测点为 `d 00:10,…,d+1 00:00`，分别对应 `d 00:00–00:10,…,d 23:50–d+1 00:00` 的 144 个物理区间。

### 采用理由

该时间轴直接反映负荷与光伏的终点语义，使相邻观测点对应的物理区间连续且不重叠。结果模板只作为输出布局，通过独立映射字段连接，不再用于定义实际数据的物理时段。

### 影响

- 源日期与日历物理区间日期一致；
- 结果模板行号与源数据列号存在一段错位；
- 0:00、24:00 SOC 从全局状态序列提取；
- 后续预报调整必须基于真实时间戳截取尚未执行区间。

### 验证

- 每个源日期恰好 144 段；
- 相邻区间严格连续；
- 首段为 `00:00–00:10`，末段为 `23:50–24:00`；
- 负荷/光伏观测时刻等于区间终点，电价时刻等于区间起点。

## 3.3 严格的信息可得性

### 含义

每天 0:00 制定计划时，只允许使用当时已经给出的附件信息和此前已经发生的实际数据。6:00、12:00、18:00 调整时，只允许加入该时刻已经发布的新预报和已经实现的系统状态。

### 采用理由

题目的核心是“提前制定策略”。如果日初计划使用当天未来实际负载或光伏，就等于提前知道未来，所得结果属于完美信息回算，不是可执行策略。

严格的信息边界还能避免：

- 紧急购电量被人为压低；
- 预测误差被错误消除；
- 问题 2 与问题 3 的差异失去意义；
- 论文被判定为数据泄漏或时序逻辑错误。

### 影响

所有预测数据必须保存 `issue_ts`、`target_ts`、`training_start` 和 `training_end`，并满足 `training_end < issue_ts`。

### 验证

为每条预测执行时间审计；任何训练记录日期不早于发布时间时，质量检查直接失败。

## 3.4 实际负载和光伏仅用于结算与回测

### 含义

附件 2 的当天实际值不进入当天 0:00 的计划输入，而用于：

- 模拟计划执行；
- 计算实际供电缺口；
- 确定紧急购电；
- 更新真实 SOC；
- 评价预测误差和策略费用；
- 进入后续日期的历史窗口。

### 采用理由

实际值是事后观测。将其用于计划会破坏信息时序，但完全不使用实际值又无法计算紧急购电和实际费用。因此应严格区分“计划输入”和“事后结算输入”。

### 验证

计划输入表不得包含同日 `load_actual` 或 `pv_actual` 的直接引用；仿真结算表则必须能够回溯到附件 2。

## 3.5 哪些问题需要预测，以及为什么需要预测

### 问题 1

题目已经给出代表性负载、电价和光伏预测，不需要额外预测。

### 问题 2

每天 0:00 需要提前申报未来 24 小时计划，但附件 2 只有实际负载和实际光伏。因此需要用此前数据估计：

- 未来负载；
- 未来光伏。

也可以从数学上直接预测净负荷，但最终选择分别预测负载与光伏，以保持物理含义清楚，并与问题 3 的数据接口一致。

### 问题 3

光伏使用附件 3 已给出的预报，不再自行预测。附件没有提供负载预报，因此仍需生成日初负载预测。

### 问题 4

问题 4 只改变价格数据，预测信息结构分别继承问题 2 和问题 3。

### 采用理由

预测只是产生决策输入的必要步骤，最终目标仍是优化购电策略。采用简单、透明的预测基线可以避免将赛题重心错误转移到复杂黑箱预测模型。

## 3.6 最近 7 天相同时段均值基线

### 含义

日期 `d` 的某个 10 分钟时段，使用 `d-7` 至 `d-1` 相同时段实际值的算术均值作为预测。

问题 2 的光伏预测采用最近 7 天相同时段均值；负载预测已统一为**周持久化** L_{d−7}（d<7 使用附件 1 曲线，仅预热期），依据与对比见 §3.8.1 及《问题二预测.docx》负载预测部分，七天均值仅保留为对照方案（b2）。问题 3 只使用负载基线。问题 2 的 7 日光伏结果作为 A1 试金石保留，正式光伏预测由独立的晴空包络×KPV 管线生成。

### 采用理由

- 严格使用历史信息；
- 基线本身不需要天气或日历特征；正式光伏模型仅额外使用年内位置 sin/cos 作为物理季节相位；
- 能跟随近期水平变化；
- 比单日持续法更稳定；
- 比全部历史均值对近期变化更敏感；
- 便于解释和复现。

### 为什么选择 7 天而不是 14 天或全部历史

14 天及全部历史会提高平滑程度，但也会削弱对短期负载与光伏水平变化的响应。7 天在稳定性和响应速度之间取得较简单的平衡，而且 2 月 1 日前已有完整历史窗口。

### 影响

该基线用于跑通完整链路，不代表最终预测方法已经锁死。后续可以在保持字段接口不变的情况下加入其他预测列。

### 验证

随机抽取日期和时段，独立读取此前 7 天原值并复算均值；同时检查窗口中没有当日或未来记录。

## 3.7 冷启动使用附件 1

### 含义

- 2025-01-01 使用附件 1 的负载和光伏代表性曲线；
- 1 月 2 日使用此前 1 天实际值；
- 1 月 3 日使用此前 2 天均值；
- 依次增加历史长度；
- 1 月 8 日起固定使用最近 7 天。

### 采用理由

1 月 1 日没有此前实际数据，必须给出可执行的初始估计。附件 1 是题目正式提供的先验信息，而且其曲线与全年相同时段均值基本一致，适合作为冷启动基准。

直接使用 1 月当天未来实际值虽然方便，但会破坏已确认的信息边界。

### 影响

1 月同时承担预测冷启动和 SOC 热身作用；正式提交从 2 月 1 日开始，此时已经具有完整 7 天历史。问题 2 负载侧采用周持久化：1 月 1 日零计划冷启动，1 月 2—7 日负载预测使用附件 1 曲线，1 月 8 日起使用 L_{d−7}；光伏侧沿用本条所述逐步扩展的历史均值。

### 验证

在预测结果中保存 `history_days` 和 `forecast_source`。1 月 1 日应标记为附件 1 先验，1 月 2～7 日应显示逐渐增加的历史天数。

## 3.8 不使用外部数据，日历变量仅保留光伏物理相位

### 含义

不引入天气、辐照度、节假日或其他外部数据；不在分析或模型中使用月份、星期几、工作日/周末等类别特征。光伏日前模型仅允许年内位置 sin/cos，用连续周期变量表达辐照的物理季节相位。

### 采用理由

- 保证方案完全基于题目附件；
- 避免外部数据地区、时间和口径无法对应；
- 保持方法简单且容易复现；
- 防止前置分析范围无控制扩张。

### 日期仍然保留的用途

日期仍必须用于时间排序、历史窗口、跨表连接、结果期标识、重点日期提取和信息泄漏检查。负载模型不使用日期解释变量；光伏模型只使用年内位置的 sin/cos 编码描述物理季节相位，不使用星期、周末或节假日信息。

### 验证

最终预测特征表不得出现月份、星期、周末、节假日等字段；分析报告也不得按这些类别分组。光伏预测表允许出现 `annual_sin`、`annual_cos`，但不得将它们转化为月份等类别变量。

### 3.8.1 修订（2026-09-12）：负载预测启用官方日历日类型

依据用户提供的《问题二预测.docx》负载预测部分，本口径在**负载预测**上更新：负载相似日模型使用 2025 官方日历的四种日类型（工作日、周六类、周日、节假日），判定规则与日期清单见 `config/data_contract.json` 的 `load_day_ahead` 段，其中 2025-04-04（清明，周五）按规则②归入周六类。调休上班日归入工作日。此用法覆盖原"负载模型禁用日历特征"约定；光伏侧仍只允许年内 sin/cos，星期、节假日特征在光伏模型中继续禁用。

## 3.9 附件 3 的预测步长解释

### 含义

对发布时间 `issue_ts`，第 `h` 个预测值的目标时刻定义为：

```text
target_ts = issue_ts + h 小时
```

因此“预报 1 小时”对应发布后一小时。

### 采用理由

将附件 3 与附件 2 实际光伏分别按“发布后 1 小时”和“发布当小时”对齐后，前者误差显著更小，能够确认预测步长语义。

### 影响

任何偏移一小时的解释都会导致预测误差、调整时点和购电区间整体错位。

### 验证

对四个发布时间分别抽样，检查预测目标时间；同时比较两个候选偏移的误差，确保采用误差合理的对齐方式。

## 3.10 小时预报线性插值到 10 分钟

### 含义

以发布时间当刻已经观测到的光伏为起点，以未来 1～24 小时整点预报为后续锚点，在相邻锚点间进行线性插值，每次发布时间生成未来 144 个 10 分钟值。

### 采用理由

- 10 分钟优化必须有同粒度输入；
- 线性插值比每小时复制常数更连续；
- 不引入额外预测模型；
- 保留官方预报整点值；
- 方法透明，便于论文说明。

### 插值约束

- 插值结果不得小于零；
- 整点处必须严格等于附件 3 原值；
- 发布时间当刻的实际光伏可以使用，因为它在决策时已经发生。

### 验证

检查每个发布时间生成 144 条记录、相邻时刻相差 10 分钟、整点锚点完全一致以及所有预测值非负。

## 3.11 问题 3 保持负载预测不变

### 含义

6:00、12:00、18:00 调整时，只更新尚未执行区间的光伏预报；负载仍使用当天 0:00 生成的预测。

### 采用理由

问题 3 明确新增的是光伏预报信息。若负载预测也同步更新，则成本变化同时受到两种新信息影响，无法判断额外光伏预报是否值得使用。

固定负载预测可以形成控制变量，使不同调整策略之间的比较具有可解释性。

### 影响

- 已执行区间不允许改变；
- 剩余区间只替换光伏预测版本；
- 负载预测版本号始终指向当日 0:00；
- 策略效果差异可归因于光伏预报更新和相应调整费用。

### 验证

对同一计划日比较四个决策版本：剩余区间的负载预测必须完全相同，光伏预测允许变化。

## 3.12 Python 数据管线

### 含义

使用 Python 完成数据读取、转换、校验、基线预测、图表和报告生成。主要依赖为 pandas、NumPy、openpyxl、PyArrow、Matplotlib 和 Seaborn。

### 采用理由

- 适合批量读取和整理 Excel；
- 长表和时间索引处理成熟；
- Parquet 能保存明确的数据类型并提高后续读取效率；
- 便于自动测试与 HTML 报告生成；
- 后续无论采用何种优化求解器，均可读取统一数据接口。

### 影响

原始附件解析只在数据管线中进行一次，后续优化代码不得重复编写各自的 Excel 清洗逻辑。

### 验证

在全新输出目录中运行管线，应能仅凭原始附件重新生成所有处理结果，并得到相同的行数、哈希和统计摘要。

## 3.13 SOC 全年连续

### 含义

储能状态从 2025-01-01 初始值开始，按照真实时间顺序持续传递，不在每日 0:00 重置。

### 采用理由

每日重置会等价于每天免费获得固定初始电量，切断前后两日的物理联系，并夸大储能的经济收益。

题面只在问题 1 中明确要求当日首末 SOC 相等，问题 2～4 更合理的处理是跨日连续。

### 影响

- 前一天末状态是下一天初状态；
- 1 月需要参与运行热身；
- 每日优化不能彼此完全独立；
- 结果表中的 0:00、24:00 SOC 应来自同一条全局状态序列。

### 验证

对每个日界线检查前一日 24:00 SOC 与后一日 0:00 SOC 完全相等。

## 3.15 充电和放电效率各为 90%

### 含义

充电效率取 0.9，放电效率取 0.9，完整充放电循环的往返效率为 0.81。

若充电量定义为交流侧进入储能的电量、放电量定义为储能向微网输出的电量，则：

- 充入 1 kWh，SOC 增加 0.9 kWh；
- 对外放出 1 kWh，SOC 减少 `1/0.9` kWh。

### 采用理由

题面“充放电效率为 90%”存在往返效率与双向效率两种可能解释。采用双向各 90% 后，能量流向和状态变化定义清楚，也便于逐项检查。

### 影响

该口径会直接影响可用电量、购电量、充放电时机和费用，因此配置文件、模型和论文必须完全一致。

### 验证

设计一组 1 kWh 充电和 1 kWh 放电的单步测试，分别核对 SOC 增量和减量。

## 3.16 调整购电采用分段净结算

### 含义

设初始计划购电量为 `plan`，最终调整后购电量为 `adjusted`，交易时刻电价为 `price`：

```text
down = max(plan - adjusted, 0)
up   = max(adjusted - plan, 0)

保留部分费用 = price × min(plan, adjusted)
下调违约费用 = 0.5 × price × down
上调新增费用 = 1.5 × price × up
```

若最终仍然供电不足，紧急购电另按 `5 × price` 计算。

### 采用理由

该口径对应“保留部分正常购买、取消部分支付违约费、新增部分按高价购买”的交易逻辑，避免原计划全额付费后再次对同一差额重复收费。

如果采用“原计划全额付费再加下调违约费”，下调通常不再具有经济意义，也不利于解释题目设置调整机制的目的。

### 影响

总费用必须分为计划保留费用、下调违约费用、上调新增费用和紧急购电费用，不能只输出一个无法复算的总数。

### 验证

分别设计不调整、仅下调、仅上调和同时包含不同区间上下调的测试，检查费用分项与总费用。

## 3.17 “调整购电量”保存最终生效计划

### 含义

官方工作表中的每个区间记录历次预报更新后最终执行的购电量。

### 采用理由

官方模板只有一张按日、按区间排列的调整表，无法自然保存 6:00、12:00、18:00 三个完整版本。保存最终量最便于执行、平衡校验和费用复算。

### 不包含

- 相对原计划的差额；
- 每次调整的完整历史表；
- 仅最后一次调整新增的数量。

调整历史应保存在内部日志中，用于论文分析和程序审计。

### 验证

检查最终量满足：原计划减累计下调、加累计上调，并与最后生效版本一致。

## 3.18 竞赛完整前置包

### 含义

前置阶段不仅完成数据清洗，还输出：

- 数据契约；
- 标准化长表；
- 日初基线预测；
- 10 分钟光伏预报；
- 数据质量报告；
- 描述性统计表；
- 前置分析图；
- 光伏预报误差分析；
- 四个重点日期核查；
- 可供优化程序直接读取的数据接口。

### 采用理由

国赛论文中的数据处理不能只写“经过预处理得到数据”。完整前置包能够形成可展示、可复算的证据，同时提前暴露时间错位、单位错误和数据泄漏问题。

### 边界

该阶段只评价数据和预报，不产生正式优化策略或问题结论。

## 3.19 数据质量检查与异常值政策

### 含义

对文件结构、时间连续性、主键唯一性、数值范围、单位转换、预测目标映射和信息边界执行自动检查。

异常值采用“标记但不自动删除”的政策。

### 采用理由

微网数据中的尖峰可能是真实的负荷高峰、光伏波动或价格波动。没有外部证据时直接平滑或缩尾，会改变供需缺口和费用结果。

夜间光伏零值属于有效观测，不能作为缺失值填补。

### 验证重点

- 主表 52,560 行；
- 正式结果期 48,096 个区间；
- 小时光伏预报长表 35,040 行；
- 10 分钟光伏预报表 210,240 行；
- 每个计划日 144 段；
- 数值字段无非法空值或无穷值；
- 所有预测训练数据早于发布时间。

## 3.20 前置分析措施

### 代表性日分析

比较附件 1 与全年相同时段均值，用于说明问题 1 的代表性输入及固定价格来源。全年均值只能用于回顾性说明，不能成为某日提前知道的实际数据。

### 负载、光伏和净负荷分析

分析 144 个时段的平均曲线、总体分布、光伏剩余比例和峰值错位。净负荷定义为负载减光伏，负值必须保留。

### 电价分析

比较附件 1 固定价格和附件 4 波动价格的峰谷差、离散程度及其与净负荷的关系，为问题 4 的结果解释准备依据。

### 预报误差分析

误差统一定义为“预测值减实际值”。计算 MAE、RMSE、偏差、绝对误差分位数以及高估、低估比例，并按预测步长和发布时间分析。

判断新增预报是否有价值时，必须比较同一交付时刻的新旧预报，不能直接比较不同发布时间的总体误差，因为它们覆盖的昼夜结构不同。

### 四个重点日期核查

单独检查 2025-03-20、2025-06-21、2025-09-23、2025-12-21 的负载、光伏、净负荷、电价和各版本预报，确保论文表格与最终结果能够从统一数据接口无歧义提取。

### 不使用日历分组

按照已确认口径，前置分析不按月份、星期或节假日分组。允许按计划时段、预测步长、发布时间和数值区间分析；光伏模型中的年内 sin/cos 仅用于连续的物理季节相位。

---

## 4. 口径之间的逻辑关系

这些口径并非彼此独立，而是形成一条完整的数据逻辑链：

```text
明确负荷/光伏终点与电价起点语义
        ↓
建立00:00至24:00的日历物理时间轴
        ↓
建立日历区间到结果模板的错位映射
        ↓
实际值只用于执行和回测
        ↓
用历史7日均值形成日初输入
        ↓
问题3用附件3更新光伏、保持负载不变
        ↓
在全年连续SOC和统一效率下执行策略
        ↓
按分段净结算计算调整费用
        ↓
将最终生效购电量写入官方模板
```

任意修改其中一项，均可能影响后续接口。例如，若改变时段映射，预测目标时刻、SOC 日界线、费用汇总和结果回填都必须同步改变；若允许使用同日实际值，则基线预测、紧急购电和问题 3 的预报价值分析都会失去原有含义。

---

## 5. 论文撰写时的使用方式

在论文“问题分析”中说明：

- 每天的计划属于提前决策；
- 实际负载和光伏只能用于事后执行；
- 问题 3 的新增信息仅为光伏预报；
- 四问共享统一的时间轴和储能状态。

在论文“数据预处理”中说明：

- 原始宽表如何转为长表；
- `0:00+1` 和跨日时段如何处理；
- 功率如何换算为电量；
- 附件 3 日期如何填充；
- 小时预报如何线性插值；
- 如何防止未来信息泄漏。

在论文“模型假设”中说明：

- 充、放电效率各为 90%；
- 不考虑向外网售电；
- 允许光伏剩余通过储能或弃光处理；
- 问题 2～4 的 SOC 按真实时间连续传递；
- 调整购电采用分段净结算。

在论文“模型评价”中说明：

- 简单基线的作用是提供可复现决策输入；
- 预报误差按相同目标时刻公平比较；
- 不将预测精度等同于最终经济效果；
- 是否采用后续预报最终应由总费用、紧急购电量和运行可行性共同判断。
