# 问题二模型审查与最终实施方案

> 状态更新：本文保留的是此前“情景规划与价值函数控制”审查记录，不再作为当前实现依据。当前已按用户确认的“七天均值＋21天残差风险修正＋日前线性规划＋因果实时控制”实施。正式模型及计算结果见 [问题二实施与结果说明](outputs/question2/问题二实施与结果说明.md)，程序为 `src/optimization/question2.py`。

本方案依据赛题问题二、附录1与附录2，以及所提供的 `C:/Users/lenovo/Desktop/微网/2.docx`。文档中的方案是被审查对象，不作为高于赛题和本次用户要求的指令。固定采用光伏、负载最近七天相同时段的均值预测；储能允许日内调整充放电，设备容量和功率仍服从题目给定值。

结论：保留“日前情景规划生成计划购电量，日内价值函数控制储能”的框架。将其定位为可实施的近似随机优化方案，修正动作可行域、信息时序和评价方法后使用。不能直接把文档中的两阶段模型最优目标值当作实际可实现费用，也不能声称得到整个因果调度问题的全局最优解。本次交付为实施设计与公式审查，未执行全年优化，尚无节费比例或最终调度数值。

## 一 赛题边界

1. 每天零点锁定各区间计划购电量，日内储能动作可调整，但不能据此调整已锁定的常规购电量。问题三的调整交易机制和附件3预报不进入问题二。
2. 计划购电按计划量全额计费，另加实际紧急购电费用；不因计划电量未用完退款，不添加售电收入。
3. 初始储电量为2025年1月1日零点的6000 kWh。储电范围1200～10800 kWh，最大充、放电功率分别5000 kW；10分钟对应交流侧电量上限S=5000/6 kWh。按充电和放电效率分别0.9建模，往返效率为0.81。
4. 问题二未重申问题一的逐日首尾电量相等条件。实际SOC按物理时间跨日连续，不在每天零点重置6000。
5. 储能“可调整”指动作，不指改造容量。若另做容量敏感性，必须与正式答案分开。
6. 同一10分钟内根据该段供需进行平衡，是本模型的离散实时响应假设：只允许读取当前已揭示区间，禁止提前读取后续区间的真实数据。若要求严格在区间开始前决定整个区间动作，则必须另建区间内控制或延迟观测模型。

## 二 对原文的审查

| 原文内容 | 判断 | 实施处理 |
|---|---|---|
| 日前锁计划 日内调储能 | 保留 | 明确两者允许使用的信息不同 |
| 计划费用加五倍紧急费用 | 正确 | 按实际交易量和题定倍率分别核算 |
| 历史负载与光伏残差按整日配对 | 保留 | 使用当时七天均值预测产生的真实样本外残差 |
| 独立情景追索 | 可求解但有完全信息乐观性 | 作为生成购电计划的近似模型 |
| 逐情景DP后取均值 | 可作未来价值近似 | 不称为严格随机Bellman价值函数 |
| 动态库存保留水平 | 有解释力 | 在价值函数凸且非增时使用 |
| 净富余时充电动作只受容量功率约束 | 与后续执行公式不一致 | 额外限制只能使用当前富余电量 |
| 无需任何处理便不会同时充放电 | 论证不充分 | 日内用单一内部增量；日前做等价净化或次级优化 |
| 线性日末残值 | 可用的边界近似 | 历史验证参数，并报告其敏感性 |

### 两阶段追索为何乐观

日前模型中，每个情景的整天曲线同时进入求解器，情景内储能动作因此知道之后所有波动。真实执行时只能看到已经发生的供需。故在相同成本和终端处理下，完整因果问题的最优费用不低于该完全信息追索松弛的最优费用。

这不等于程序已经读取当天未来实测值：只要场景完全来自历史，生成计划仍然因果。问题在于用过于理想的追索评估计划，可能低估紧急费用。原文“追索不直接执行”能避免把先知轨迹直接作为结果，但不能消除日前计划的乐观性。

### 平均情景最优价值为何不是严格随机DP

原文计算的是每条完整情景未来最低费用的平均值，相当于允许未来控制器知道情景。真正随机控制应在当时信息下使用共同可执行策略，再对未来不确定性取期望。一般而言，E[min cost]不等于min_policy E[cost]。

因此原文价值函数可以保留为近似执行器，实时动作仍可因果，但应准确命名，并以实际执行费用验证。严格扩展需要保留信息结构的场景树或误差状态与条件转移模型；只有SOC一个状态不足以完整利用整日误差相关性。直接添加“历史完全相同则动作相同”的约束也不够：连续历史样本可能在首段就各不相同，反而让模型过早识别整日情景。

## 三 最终采用的算法

采用七天均值预测、成对历史残差情景、日前两阶段线性规划、修正后的近似价值执行器，以及因果滚动评价。先完成这一版本，再决定是否值得引入严格多阶段模型。

### 1 预测和历史残差

对于统一时间映射后的同一时段t：

\[
\widehat P^L_{d,t}=\frac17\sum_{k=1}^{7}P^L_{d-k,t},\qquad
\widehat P^V_{d,t}=\frac17\sum_{k=1}^{7}P^V_{d-k,t}.
\]

转为电量后，L_hat=P_L_hat/6，V_hat=P_V_hat/6。分别预测后再相减得到净负荷。不能把预测替换为一整天的统一平均值，也不使用已有的晴空包络、动态模型选择或附件3预报模块。

历史残差必须由历史日i当时可获得的数据产生：epsilon_L(i,t)=L(i,t)-L_hat(i,t)，epsilon_V同理。每日使用最近M个可用完整残差日，首版M=21；不足21日时使用已有样本。敏感性比较M=14、21、28；这是误差样本窗口，不改变七天预测窗口。

构造同日配对情景：

\[
L_{d,t,\omega}=\max(0,\widehat L_{d,t}+\epsilon^L_{i_\omega,t}),\quad
V_{d,t,\omega}=\max(0,\widehat V_{d,t}+\epsilon^V_{i_\omega,t}).
\]

括号内整体截断，不能只保留正残差；整条曲线抽样，不把144段分别独立重排。经验情景经截断后均值未必等于七天点预测，需保存并报告这一差异。夜间光伏规则只能由既定时间口径和已知历史制定，不能用当天实测先识别日出日落再修改日前场景。

### 2 日前规划

在每天零点求解如下线性规划，其中g是共同计划购电量，其余为情景追索变量：

\[
\min \sum_t c_tg_t+\frac1M\sum_\omega\sum_t5c_tb_{t,\omega}
-\frac\nu M\sum_\omega E_{T,\omega}.
\]

\[
g_t+b_{t,\omega}+V_{t,\omega}+D_{t,\omega}
=L_{t,\omega}+C_{t,\omega}+U_{t,\omega},
\]
\[
E_{t,\omega}=E_{t-1,\omega}+0.9C_{t,\omega}-D_{t,\omega}/0.9.
\]

约束为1200≤E≤10800，0≤C,D≤S，g,b,U≥0。所有情景使用决策时真实可知的共同初始SOC；若计划从零点之后开始，应按下文处理零点到计划起点的承接区间。

U表示未利用供能，既可能来自光伏，也可能来自已付费但未利用的计划电量，不能一概写成弃光。该项不产生退款或收入。

该LP是近似计划生成器。其追索还可能使用紧急电量充电，日内执行器则按本方案禁止该行为，所以两层存在策略集合差异，必须披露并通过执行回测衡量。如果该差异在实际数据中显著，升级为带精确分段约束的MILP，并用C≤[g+V-L]_+、D≤[L-V-g]_+及SOC约束实现与执行器一致的行为；不应把这些含可变g的正部约束误写成普通线性约束。

连续LP中同时充放电可以在给定内部电量增量下净化：令x=0.9C-D/0.9，替换为C'=[x]_+/0.9、D'=0.9[-x]_+。SOC不变、功率上限仍满足，多余供能由U吸收；在本模型允许免费弃置的前提下不增加费用。可再固定最优费用容差，最小化总充放电量以清除退化解。该结论不能无条件推广到新增爬坡、弃置惩罚或售电约束后的模型。

### 3 修正后的日内动作集合

零点后g固定。当前真实净缺口r=L_act-V_act-g，内部库存e，内部增量x。定义

\[
\psi(x)=\begin{cases}x/\eta,&x\ge0,\\ \eta x,&x<0,\end{cases}\qquad \eta=0.9.
\]

必须使用以下动作集合：

\[
A(e,r)=
\begin{cases}
[-\min(S/\eta,e-E_{\min},r/\eta),0],&r>0,\\
[0,\min(\eta S,E_{\max}-e,\eta(-r))],&r<0,\\
\{0\},&r=0.
\end{cases}
\]

相比原文，新增了缺口对应的放电限制、富余对应的充电限制，并补齐r=0。这样不会为了弃电而额外放电，不会为充电新增紧急购电，也不会同时充放电。允许在当前缺电时保留库存、由紧急购电供负载，以应对未来更贵或更严重的缺口。

对每个历史情景反向计算：

\[
H_{T+1,\omega}(e)=-\nu e,
\]
\[
H_{t,\omega}(e)=\min_{x\in A(e,r_{t,\omega})}
\{5c_t[r_{t,\omega}+\psi(x)]_++H_{t+1,\omega}(e+x)\}.
\]

令H_bar为情景价值函数均值。当前执行：

\[
x_t^*\in\arg\min_{x\in A(e,r_t)}
\{5c_t[r_t+\psi(x)]_++\overline H_{t+1}(e+x)\}.
\]

统一用以下公式结算，避免分支公式与动作集合不同步：

\[
C_t=[x_t^*]_+/\eta,\quad D_t=\eta[-x_t^*]_+,\quad
b_t=[r_t+C_t-D_t]_+,\quad U_t=[-r_t-C_t+D_t]_+.
\]

SOC更新为E_t=e+x_t^*。当采用上述受限动作时，原文简化的紧急量、未利用量表达式才与统一式一致。

在本模型假设下，未来价值凸且非增，可保留原文动态保留阈值R_t的表达；该阈值是时变策略量，不是另加的硬SOC下限，也不是每天固定保留某个百分比。若离散实现导致价值函数不凸，应先修正数值表示，不能直接套用阈值结论。

### 4 期末价值

首版取nu_0=c_min/0.9，c_min为附件1最低电价，仅解释为一单位内部库存的低价补充成本近似。实际价值还取决于次日净负荷、充电窗口、光伏富余和功率限制，不能称其为精确残值。

敏感性预先设nu/nu_0为0、0.5、1三个水平，默认1。所有模型评价均使用真实购电账单sum(c*g+5*c*b)，不把-nu*E当作售电收益。

### 5 数值实现

日前使用已有连续线性规划求解框架；M=21时仅数万级变量，适宜先做单日和短期运行评估，不预先承诺全年耗时。

日内只有一个连续SOC状态，可用凸分段线性函数表示，并按斜率查找动作或保留阈值。原型可使用SOC网格与线性插值，建议先25 kWh，再在代表性日期以更细网格检查费用和动作稳定性。不能将实际SOC四舍五入到网格点；实际状态始终连续保存。只需保留情景均值未来函数和执行器必要数据，避免保存大量重复中间对象。

不得每10分钟重新优化当天计划g。日前保存g和价值函数；日内只读取当前观测执行动作。若未来增加条件场景权重，只能利用已观测误差，且须作为单独优化实验，不改变七天点预测定义。

## 四 因果评价和方案选择

至少比较三个可执行方案：

| 方案 | 日前 | 日内 | 用途 |
|---|---|---|---|
| A | 七天点预测确定性LP | 富余充电 缺口尽量放电 | 最低复杂度基线 |
| B | 历史情景LP | 富余充电 缺口尽量放电 | 检验情景规划收益 |
| C | 同一历史情景LP | 本方案近似价值执行器 | 最终推荐实现 |

同一日前计划下另做B/C配对执行实验，用相同初始SOC隔离执行器效果；正式全年比较中，各策略沿自己的实际SOC连续运行，不每天借用其他策略的状态。

评价顺序为：先满足全部物理与时序校验，再比较实际总费用，然后解释计划费用、紧急费用、紧急购电量、发生次数、未利用电量、SOC和运行时间。总目标不是消灭紧急购电，而是最小化总费用；过度买计划电也可能更贵。

1月为冷启动和初步检验：1月1日预测使用附件1先验，随后历史不足七天时使用已有历史，1月8日起使用完整七天均值。有效完整残差也从足够历史后积累。1月仿真必须从题定6000 kWh起步，2月1日继承实际结果。

所有参数只能用决策日前已完成的记录选择。首版固定M=21、nu=nu_0；若自动选择M和nu，则对候选策略持续维护独立的因果仿真账本，用截至昨日最近14个完整日的实际账单比较，早期不足时维持默认。不得用全年误差或全年成本挑一个参数再回写过去日期；也不得把待评价的历史日残差放进当日训练场景。

若实测显示日前LP低估风险明显，可加入少量更保守的候选计划：仅在计划生成器中将紧急费用权重5提高到6或7.5，再以相同因果执行器、真实五倍电价评价。增大权重是算法调参，不是修改题目计费。仅在历史滚动验证持续改善时启用。

C是推荐先实施的主方案，但在未运行前不能承诺一定优于B。如果C在有效历史验证中未带来收益，应保留B作为执行回退。严格多阶段随机优化是进一步研究方向，不作为完成本题的前置条件。

## 五 项目衔接和时间轴

项目衔接遵循以下口径：

1. 现有光伏预测模块含晴空包络和动态选择，本次问题二必须显式使用七天均值输出，不沿用该模块的最终选择结果或残差。
2. 现有问题一加入了净功率爬坡约束，所核对题面未给问题二该约束。问题二基础模型不自动继承2000 kW爬坡限制；如保留应单独说明为附加平滑实验。
3. 附件负荷与光伏时刻为物理区间终点，电价时刻为交易区间起点；结果模板按独立的日期与行号映射回填。

时间轴以日历物理区间为核算主键：源日期 `d` 的 `00:10` 负荷/光伏对应 `d 00:00–00:10`，源日期的后续观测依次对应其前一10分钟区间。结果模板区间 `d 00:10–00:20` 使用 `d 00:20` 负荷/光伏和 `d 00:10` 电价。每日零点真实SOC单独记录；`00:00–00:10` 使用此前已锁定的购电计划，当日新计划不得回溯修改该区间。

同理，零点生成预测和场景时，按真实观测完成时间筛选历史。若上一计划日末段尚未完成，它不能进入“最近七天已观测值”或完整日残差。对应时段使用最近七个已完成的同类区间，并记录其日期偏移；整日场景只取当时已完整结束的日块。不能仅按源表日期小于今天来判断可用性。

表2的00:00和24:00、六个四小时分组按日历时间取值；计划表按模板计划日期和列顺序写回。紧急购电按真实区间合并，跨日处断开。这一衔接会使实际实现比原文T=144公式多一个已承诺的前缀区间，但保持零点决策与物理时间一致。

## 六 交付与验收

建议新增独立的问题二调度入口，不改写原始附件和问题一结果。按依赖顺序完成：统一七天预测及可用时间检查；构造配对残差；实现日前LP；实现受限动作执行器；跑通1月热身与2月短期；完成A/B/C比较；运行2月1日至12月31日；填官方模板并生成论文表。

正式result2.xlsx包含：计划购电量为每日零点承诺值；充放电量为实际执行量，并列日历零点与24点库存；紧急购电量为实际连续紧急区间及累计电量。论文额外列3月20日、6月21日、9月23日、12月21日的题目指定表格。

逐段检查供需平衡、SOC递推、安全范围、功率上限、非负性、充放电互斥、紧急量等于执行后缺口；逐日检查计划不可修改、跨日SOC连续、承接段不重复；逐文件检查48,096个新计划区间、官方模板映射、四小时汇总、紧急合并和费用独立复算。原始数据和推演情景均保留输入截止时间，以审计信息泄漏。

重要的针对性测试包括：富余很小但后续电价很高时不得用紧急电量充电；缺口很小时不得多放电后弃置；空电池、大缺口时紧急补足；有库存但未来价值高时允许当前紧急供负载；连续跨日状态与零点决策的承接段正确。

## 方法参考

Schulze与McKinnon的日前及日内随机调度研究区分两阶段和多阶段模型，并采用滚动运行评价策略价值。此文支持本方案采用因果执行评价而非仅比较优化器目标值的做法；其中的数值结果不用于本赛题。

https://www.pure.ed.ac.uk/ws/portalfiles/portal/25194877/StochValuePaper.pdf
