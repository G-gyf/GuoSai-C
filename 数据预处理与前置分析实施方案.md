# 数据预处理与前置分析实施方案

## 一、目标与范围

建立一套可复用、可审计的数据管线，为四个问题提供统一的时间轴、单位、预测输入和质量检查。

本阶段包括：

- 原始数据读取、格式统一和长表化；
- 严格遵守信息可得性的基线预测；
- 光伏小时预报转为 10 分钟预报；
- 数据质量、净负荷、电价与预测误差分析；
- 输出优化可直接读取的数据表及前置分析报告。

本阶段不包括：

- 购电和储能优化模型；
- 最终求解及结果模板回填；
- 日历、节假日、天气等外部特征；
- 使用同日实际值生成计划。

## 二、已对齐的统一口径

| 事项 | 确认口径 |
|---|---|
| 数据来源 | 只使用题目附件，不接入外部数据 |
| 日历特征 | 光伏日前模型仅允许年内位置 sin/cos 作为物理季节相位；星期、节假日和外部日历特征禁用 |
| 负荷与光伏时刻 | 附件中的时刻表示前一 10 分钟物理区间的终点 |
| 电价时刻 | 附件中的时刻表示购电交易区间的起点 |
| 日历物理时域 | 源日期 `d` 的 144 个负荷/光伏值对应 `d 00:00` 至 `d+1 00:00` |
| 模板映射 | 结果行 `d` 的 `00:10–00:20` 使用源日期 `d` 的 `00:20` 负荷/光伏，依此顺移；模板映射独立于日历物理表保存 |
| 日计划汇总 | 按结果表同一日期行的 144 个区间求和 |
| SOC 汇总 | 按真实日历时刻提取 0:00 和 24:00 状态 |
| 功率转电量 | 10 分钟电量为功率乘以 `1/6` |
| 信息边界 | 每次计划只使用决策时点已经获得的数据 |
| 实际负载、光伏 | 仅用于执行仿真、费用结算和回测 |
| 日初基线预测 | 使用最近 7 天相同时段实际值的均值 |
| 冷启动 | 2025-01-01 为冻结初始条件日（无计划、电池不动作、SOC 恒 6000 kWh、不入统计，日初基线无预测）；1/2–1/7 负载退化为昨日持久化 L_{d-1}、光伏用已有历史均值；1/8 起负载用周持久化 L_{d-7}、光伏用完整七天均值；任何冷启动不使用附件 1（2026-09-13 修订） |
| 问题 2 光伏 | 7 日相同时段均值保留为基线；正式预测采用晴空包络×KPV，并按截至昨日的滚动误差在 A1～A4/集成间因果选择 |
| 问题 3 光伏 | 使用附件 3 的光伏预报，不另建光伏预测 |
| 问题 3 负载 | 使用 0:00 生成的负载预测，日内调整时保持不变 |
| 预报步长 | “预报 1 小时”对应发布时间后 1 小时 |
| 小时预报转换 | 使用线性插值转为 10 分钟粒度 |
| 插值起点 | 使用发布时间当刻已经观测到的光伏功率作为起始锚点 |
| 储能效率 | 充电效率和放电效率分别为 90% |
| SOC 连续性 | 从 2025-01-01 初始 6000 kWh 开始连续传递 |
| 调整结算 | 保留量按正常价、取消差额付 50% 违约费、新增差额按 1.5 倍计价 |
| 调整购电量表 | 保存各时段最终调整后的购电计划，不保存差额 |

## 三、实施清单

### 1. 原始数据保护与登记

- [ ] 保持 `C题.pdf` 和所有附件只读，不覆盖原文件。
- [ ] 记录每个输入文件的文件名、大小、工作表、行列数和哈希值。
- [ ] 建立 `config/data_contract.json`，集中保存字段、单位、时段口径、结果期和参数。
- [ ] 将原始文件结构检查与业务数据检查分开记录。

已知规模应固定为：

- 附件 1：144 个代表性日时点；
- 附件 2：365 天 × 144 时段 × 2 个工作表；
- 附件 3：365 天 × 4 个发布时间 × 24 个预测步长；
- 附件 4：365 天 × 144 个电价时段；
- 正式结果期：334 天 × 144 时段。

### 2. 统一时间轴

建立全局 10 分钟时间轴，核心字段如下：

| 字段 | 含义 |
|---|---|
| `plan_date` | 附件负荷/光伏源行所属日历日期 |
| `slot_index` | 1～144，对应源行终点 `00:10` 至 `0:00+1` |
| `interval_start` | 区间真实开始时刻 |
| `interval_end` | 区间真实结束时刻 |
| `observation_ts` | 负荷与光伏观测时刻，等于 `interval_end` |
| `calendar_date` | 物理区间开始时刻所属日历日期 |
| `template_plan_date` | 该物理区间写入结果模板时对应的计划日期 |
| `template_slot_index` | 该物理区间写入结果模板时对应的行号 |
| `is_cross_day` | 区间终点是否落在源日期次日 |
| `is_result_period` | 是否位于 2025-02-01 至 2025-12-31 |

处理要求：

- [ ] 混合的 Excel 时间对象和文本时间统一解析。
- [ ] 将 `0:00+1` 显式转换为次日 0:00。
- [ ] 验证每个源日期恰好有 144 个连续物理区间。
- [ ] 验证相邻区间间隔恒为 10 分钟。
- [ ] 单独校验 `00:10` 终点、`23:50` 起点和 `0:00+1` 终点。
- [ ] 单独校验日历物理区间与结果模板的日期、行号错位映射。
- [ ] 不直接依赖 Excel 列号进行日期计算。

### 3. 构建实际运行主表

生成 `data/processed/dispatch_10min.parquet`，预计 52,560 行。

主要字段：

- `plan_date`
- `slot_index`
- `interval_start`
- `interval_end`
- `load_actual_kw`
- `pv_actual_kw`
- `net_load_actual_kw`
- `load_actual_kwh`
- `pv_actual_kwh`
- `net_load_actual_kwh`
- `price_fixed_yuan_per_kwh`
- `price_variable_yuan_per_kwh`
- `is_result_period`

规则：

- [ ] 附件 1 电价按物理区间起点对齐后复制到全年，作为问题 1～3 的固定价格。
- [ ] 附件 4 电价按物理区间起点对齐，作为问题 4 的波动价格，并保留来源可用性标志。
- [ ] 附件 2 负荷与光伏按物理区间终点对齐，不改变原始数值。
- [ ] 负荷与光伏功率乘以 `1/6` 得到区间电量。
- [ ] 净负荷统一定义为负载减光伏。
- [ ] 负净荷保留，不截断，表示存在光伏剩余。
- [ ] 原始数值不做平滑、缩尾或异常值替换。

### 4. 整理附件 3 光伏预报

先生成小时长表 `pv_forecast_hourly.parquet`，预计 35,040 行。

字段包括：

- `issue_date`
- `issue_time`
- `issue_ts`
- `horizon_hour`
- `target_ts`
- `pv_forecast_kw`

处理规则：

- [ ] 将附件 3 的空字符串日期转为空值并向下填充。
- [ ] 将发布时间解析为 0:00、6:00、12:00、18:00。
- [ ] 设置 `target_ts = issue_ts + horizon_hour`。
- [ ] 验证每个发布时间均有 24 个预测值。
- [ ] 验证四个发布时间各有 365 条记录。

再生成 `pv_forecast_10min.parquet`，预计 210,240 行：

- 每个发布时间生成未来 144 个 10 分钟预测点；
- 发布时间当刻的已观测光伏作为起点；
- 发布时间后第 1～24 小时使用附件 3 数值作为整点锚点；
- 锚点之间采用线性插值；
- 插值结果小于零时截为零；
- 不利用目标时刻之后的实际光伏；

### 5. 构建日初基线预测

生成 `day_ahead_baseline_10min.parquet`。

#### 冷启动（2026-09-13 修订）

- 2025-01-01：冻结初始条件日，不生成计划、不执行电池动作，日初基线预测为空（NaN）并跳过统计；
- 2025-01-02 至 2025-01-07：负载退化为昨日持久化 L_{d-1}，光伏使用此前已有日期的相同时段均值；
- 2025-01-08 起：负载使用周持久化 L_{d-7}，光伏使用完整七天同时段均值；
- 任何冷启动不使用附件 1 的负载与光伏曲线。

#### 各问题使用方式

- 问题 1：直接使用附件 1，不调用滚动基线。
- 问题 2、4-2：负载采用周持久化（d<7 昨日持久化），光伏采用七天同时段均值。
- 问题 3、4-3：负载采用周持久化；光伏使用附件 3 的相应发布时间预报。
- 6:00、12:00、18:00 调整时，只替换剩余区间的光伏预报，负载预测保持为 0:00 版本。

每条预测记录必须带有：

- `issue_ts`
- `target_ts`
- `training_start`
- `training_end`
- `history_days`
- `load_forecast_kw`
- `pv_forecast_kw`
- `forecast_source`

自动检查 `training_end < issue_ts`，防止未来数据泄漏。

### 6. 数据质量检查

生成 `outputs/preanalysis/quality_report.json` 和 `quality_checks.csv`。

检查项目：

- [ ] 文件、工作表、字段和维度符合数据契约。
- [ ] 日期、时段和组合主键无重复。
- [ ] 负荷、光伏、净负荷与固定电价字段没有空值、无穷值或非法文本；波动电价可用性标志与源数据覆盖一致。
- [ ] 负载、光伏和电价均非负。
- [ ] 光伏零值保留为有效观测。
- [ ] 每日 144 个区间完整且连续。
- [ ] 固定价格与附件 1 按区间起点一致。
- [ ] 波动价格与附件 4 按区间起点逐日期、逐时段一致。
- [ ] 负荷与光伏时刻严格等于物理区间终点。
- [ ] 日历物理区间到结果模板的日期和行号映射正确。
- [ ] 功率转电量前后相差严格为 `1/6`。
- [ ] 附件 3 预测目标时刻与实际时刻正确连接。
- [ ] 插值结果在整点处严格还原原始预测值。
- [ ] 正式结果期共有 48,096 个计划区间。
- [ ] 所有预测训练数据早于决策时刻。

异常值只标记、不删除。报告中列出位置、数值和判断依据，由后续建模阶段决定是否需要稳健处理。

## 四、前置分析清单

前置分析不按月份、星期或节假日分组，只围绕时段、数值分布、预测步长和信息更新时间展开。唯一例外是光伏日前模型可使用年内位置 sin/cos，且只解释太阳辐照的物理季节相位。

### 1. 代表性日与数据关系

- [ ] 比较附件 1 与全年相同时段均值。
- [ ] 说明附件 1 的负载和电价基本是全年平均日曲线。
- [ ] 说明附件 1 光伏仅在黎明、傍晚极小值处与全年均值存在置零差异。
- [ ] 明确附件 1 在问题 1 中是确定性代表日，在后续问题中主要提供固定价格。

### 2. 负载、光伏和净负荷

- [ ] 绘制 144 时段平均负载、平均光伏和平均净负荷曲线。
- [ ] 统计负载、光伏、净负荷的最小值、最大值、均值和分位数。
- [ ] 统计光伏高于负载的时段比例和剩余电量规模。
- [ ] 分析负荷峰值、光伏峰值与净负荷峰值的时段错位。
- [ ] 统计每日负载电量、光伏电量和净负荷电量的总体分布，但不按月份、星期分类。

已有初步事实：全年约 19.3% 的 10 分钟时段光伏高于负载，储能充电和弃光处理不可省略。

### 3. 电价特征

- [ ] 绘制附件 1 固定价格曲线。
- [ ] 分析附件 4 波动价格的总体分布、峰谷差和时段波动范围。
- [ ] 计算电价与负载、光伏、净负荷的相关关系。
- [ ] 统计高价时段与净负荷高峰的重合程度。
- [ ] 对比固定价格与波动价格的离散程度，为问题 4 的结果解释准备依据。

### 4. 光伏预报质量

误差统一定义为“预测值减实际值”。

至少计算：

- MAE、RMSE、偏差；
- 绝对误差分位数；
- 低估与高估比例；
- 按预测步长 1～24 小时的误差；
- 按 0:00、6:00、12:00、18:00 发布时间的误差；
- 全时段误差与光伏非零时段误差；
- 同一交付时刻的新预报相对旧预报的误差改善。

评价新增预报是否有价值时，必须比较同一交付区间，不能直接比较四个发布时间的总体误差，因为各发布时间覆盖的昼夜结构不同。

已有核对结果表明，“预报 1 小时”对应发布时间后 1 小时；预测误差总体随预测步长增加。

### 5. 必交日期核查

对以下四天单独输出数据摘要：

- 2025-03-20
- 2025-06-21
- 2025-09-23
- 2025-12-21

每个日期检查：

- 负载、光伏和净负荷曲线；
- 固定价格和波动价格；
- 0:00 光伏预报及后续更新；
- 不同发布时间对同一交付时刻的预报变化；
- 原始数据与最终时间索引的映射。

这一步只做数据核查，不提前解释优化结果。

## 五、交付目录

```text
config/
└─ data_contract.json

src/data_pipeline/
├─ ingest.py
├─ build_timeline.py
├─ build_actuals.py
├─ build_forecasts.py
├─ run_preanalysis.py
└─ validate_data.py

data/processed/
├─ dispatch_10min.parquet
├─ pv_forecast_hourly.parquet
├─ pv_forecast_10min.parquet
└─ day_ahead_baseline_10min.parquet

outputs/preanalysis/
├─ preanalysis.html
├─ quality_report.json
├─ tables/
│  ├─ data_inventory.csv
│  ├─ profile_summary.csv
│  ├─ forecast_error_by_horizon.csv
│  ├─ forecast_update_gain.csv
│  └─ key_dates_summary.csv
└─ figures/
   ├─ average_dispatch_profile.png
   ├─ net_load_surplus.png
   ├─ fixed_price_profile.png
   ├─ variable_price_distribution.png
   ├─ daily_energy_distribution.png
   ├─ forecast_error_by_horizon.png
   ├─ forecast_update_gain.png
   └─ key_dates_profiles.png

tests/
└─ test_data_pipeline.py
```

README 只增加前置分析报告入口和当前阶段状态，不把详细分析重复写入 README。

## 六、验收标准

实施完成后应同时满足：

1. 所有原始附件保持不变。
2. 清洗后实际运行主表恰好有 52,560 条记录。
3. 每个源日期有且仅有 144 个连续日历物理区间。
4. 附件 3 小时长表有 35,040 条记录，10 分钟预报表有 210,240 条记录。
5. 所有功率、电量、价格字段单位明确且可追溯。
6. 预测基线严格只使用决策时点之前的数据。
7. 问题 3 的负载预测固定，光伏预报可按发布时间替换。
8. 整点插值值与附件 3 完全一致，非整点值连续且非负。
9. 正式结果期完整覆盖 2025-02-01 至 2025-12-31。
10. 四个指定日期均能从统一数据表无歧义提取。
11. 所有质量检查均通过，或在报告中列出明确、未被静默修复的问题。
12. 前置分析报告不包含优化策略、求解结果或使用未来实际值形成的决策。
